# 内容智能白板｜本地运行说明

## 一、运行环境

Windows 10/11，并提前安装：

- Python 3.11 或 3.12；安装时勾选“Add Python to PATH”；
- Node.js 20.19 或更高版本；
- 首次安装依赖和配置大模型时需要网络连接；
- 不需要 Docker。

可在 PowerShell 中检查：

```powershell
python --version
node --version
npm --version
```

## 二、首次安装

1. 将压缩包解压到本地英文路径，例如 `D:\Content_Intelligence_Canvas`；
2. 双击根目录的 `首次安装.bat`；
3. 等待 Python 和前端依赖安装完成。

不要在压缩包内部直接运行，也不建议放到 OneDrive 等实时同步目录。

## 三、启动

双击 `启动内容智能白板.bat`。启动后会出现两个 PowerShell 窗口，分别运行后端和前端。

浏览器访问：<http://localhost:5173>

停止时关闭这两个 PowerShell 窗口即可。项目、白板、设置和索引保存在根目录的 `data` 文件夹。

## 四、加载随包 Wiki

1. 打开左侧“设置”；
2. 知识库路径选择或填写解压目录下的 `fixtures\wiki`；
3. 点击“刷新索引”；
4. 完成后进入“知识检索”或“快速研究”。

也可以填写自己的 Markdown / Obsidian Wiki 绝对路径。知识源按只读方式访问，不会修改原文件。

## 五、自定义大模型

在“设置”中填写服务名称、Base URL、模型名称和 API Key，然后点击健康检查。

- 支持 OpenAI-compatible 接口；
- API Key 仅保存在本机 `data\secrets.json`；
- “内部数据保护”默认开启；
- 外部模型在保护开启时不会接收内部 Wiki 或白板内容；
- 不配置大模型也能用本地模板完整体验主要流程。

## 六、推荐验收顺序

设置知识库并刷新索引 → 新建项目 → 知识检索并展开卡片 → 加入白板 → 快速研究 → 白板编辑和连线 → 生成内容概念 → 生成三类内容资产 → 导出。

## 七、常见问题

- `python` 找不到：重新安装 Python，并勾选加入 PATH；
- `npm` 找不到：安装新版 Node.js 后重新打开窗口；
- 端口被占用：关闭占用 8000 或 5173 端口的程序后重试；
- 页面打不开：确认启动后出现的两个窗口都没有报错；
- 重新安装依赖：再次双击 `首次安装.bat`，不会删除 `data` 中的本地项目数据。
