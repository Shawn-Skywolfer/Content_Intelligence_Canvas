# Local validation data

Place an extracted, read-only copy of the supplied Wiki under `fixtures/wiki/`, or pass any local Markdown vault path to the CLI/API. The real Wiki is intentionally excluded from the public Git repository because it is a knowledge source, not application source code.

